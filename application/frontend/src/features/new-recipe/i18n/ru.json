{
  "validation": {
    "name": {
      "required": "Название обязательно",
      "maxLength": "Имя превышает максимальную длину ({{length}}/{{maxLength}})"
    },
    "description": {
      "maxLength": "Описание превышает максимальную длину ({{length}}/{{maxLength}})"
    },
    "formula": {
      "required": "Инструкция обязательна",
      "maxLength": "Инструкция превышает максимальную длину ({{length}}/{{maxLength}})"
    },
    "kcal": {
      "min": "Значение меньше минимального: {{min}}",
      "max": "Превышено максимальное значение: {{max}}"
    },
    "portionWeight": {
      "min": "Значение меньше минимального: {{min}}",
      "max": "Превышено максимальное значение: {{max}}"
    },
    "cookingTime": {
      "min": "Значение меньше минимального: {{min}}",
      "max": "Превышено максимальное значение: {{max}}"
    }
  }
}
