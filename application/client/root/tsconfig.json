{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "experimentalDecorators": true, // we use decorators from redux-controller-middleware, and ts-loader still does not support stage 3 decorators
    "baseUrl": "src"
  }
}
